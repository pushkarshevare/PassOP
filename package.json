{
  "name": "passop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run dev", // Alias for the development server
    "dev": "node ./node_modules/.bin/vite", // Starts the development server using Node.js
    "build": "node ./node_modules/.bin/vite build", // Builds the project for production
    "serve": "npx serve -s dist", // Serves the production build using the 'serve' package
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0" // Runs linting
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-toastify": "^10.0.5",
    "uuid": "^9.0.1",
    "serve": "^14.2.0" // Added 'serve' for hosting production builds
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.34.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "vite": "^5.2.0"
  }
}
